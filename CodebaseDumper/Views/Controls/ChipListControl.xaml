<!-- CodebaseDumper/Views/Controls/ChipListControl.xaml -->
<UserControl x:Class="CodebaseDumper.Views.Controls.ChipListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="120" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Danh sách chip hiện tại -->
        <ItemsControl Grid.Row="0"
                      ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="4" 
                            Padding="4,2" Margin="2" Background="#F0F0F0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                            <!-- Nút xoá chip, thuộc tính AutomationProperties.Name mô tả hành động -->
                            <Button Content="×" 
                                    Width="20" Height="20"
                                    FontSize="12" FontWeight="Bold"
                                    Padding="0"
                                    Command="{Binding RemoveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    AutomationProperties.Name="{Binding ., StringFormat='Remove {0}'}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Ô nhập liệu để thêm chip mới -->
        <TextBox Grid.Row="1" x:Name="AddTextBox"
                 Margin="2,4,2,2"
                 VerticalAlignment="Bottom"
                 KeyDown="AddTextBox_KeyDown"/>
    </Grid>
</UserControl>